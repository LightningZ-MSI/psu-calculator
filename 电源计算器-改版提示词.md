# 台式机功耗计算器 —— 改版提示词包（排版简化 + 华硕奥创风格）

> 目标站点：https://lightningz-msi.github.io/-/
> 用途：把现有页面从「信息堆叠的工程师工具」改造成「奥创（Armoury Crate / ROG）风格的向导式工具」。
> 用法：先看「使用建议」，再按需复制对应提示词整段发给 AI（Claude / GPT / DeepSeek 均可）。
> 每个提示词都是**自包含**的：已内置项目背景、文件结构、类名清单和硬性约束，不需要你额外解释。

---

## 0. 项目事实速查（提示词已内置这些信息，你也可用来核对）

**文件结构**
```
index.html              # 541 行，静态骨架，页面主体由 JS 渲染
assets/style.css        # 401 行，纯 CSS 无框架，CSS 变量驱动主题
js/db-cpus.js           # 先注册 CPU 数据
js/db-aib.js            # 再注册显卡 AIC 板型数据
js/db.js                # 装配数据库
js/engine.js            # 计算引擎
js/app.js               # 56KB，渲染 + 事件 + 导出
favicon.svg
```
**加载顺序不可调换**：db-cpus → db-aib → db → engine → app。

**核心 DOM 结构**
```
header.top > .top-inner > .logo(.bolt) / .top-actions(#dbCounts,#btnPreset,#btnReset,#btnTheme)
.wrap
  .banner                                    # 数据可靠性声明（字号小、信息密）
  .layout (grid 1fr 1fr)
    .col-config                              # 左侧 10 张配置卡
      #guideCard.card.guide                  # 新手引导 + details.glossary 术语表
      section.card × 10                      # 场景/CPU/GPU/主板/内存/存储/散热/机箱/扩展/电源
    .col-result
      .sticky-col                            # 右侧结果列，sticky top:74px
        .print-only #printMeta
        .hero  > div ×3  (.k / .v.accent|.blue|.warn / .u)   # 规划/预期/瞬时峰值
        .reco  (#recoBig .big / #recoSub / #recoMeta)        # 推荐电源功率
        section.card #psuPicks/.psu-pick                      # 三档推荐
        section.card #issues                                  # 兼容性与风险
        section.card #upgradeBox                              # 升级空间
        section.card #adviceBox                               # 配置平衡建议
        section.card table.detail#detailTable                 # 功耗明细
        section.card 导出按钮 #btnCsv/#btnPdf/#btnJson
        section.card ul.src-list#sources                       # 数据来源
  section.card#feedbackCard                  # 反馈/提交硬件表单
.toast#toast
```
**关键 CSS 类**：`.card / .card-body / .card > h2 / .idx / .hint`、`.field / .unit`、`.grid2 / .grid3`、`.switch / .track`、`.scenarios / .scenario(.on)`、`.seg / .seg button(.on)`、`.hero`、`.reco`、`.tag(.on/.blue/.warn)`、`.psu-picks / .psu-pick`、`.issue(.error/.warn/.info/.ok)`、`.conf(.official/.review/.estimate/.leak)`、`.grp`、`.storage-row / .del`、`.chk-grid / .chk`、`.note(.warn/.accent)`、`.banner`、`.src-list`、`.kv`、`.empty`、`.toast(.show/.err)`、`.glossary`、`.print-only`。

**主题机制**：`html[data-theme="light"]` 覆盖 `:root` 变量（`--bg --bg-soft --panel --panel-2 --border --border-soft --text --text-dim --text-mute --accent --accent-dim --blue --warn --error --purple --radius --radius-sm --mono --shadow`）。`#btnTheme` 负责切换。

**必须保住的交互**：`#btnPreset` `#btnReset` `#btnTheme` `#btnQuickStart` `#btnHideGuide` `#addStorage` `#addCustomItem` `#fbAdd` `#fbCopy` `#btnCsv` `#btnPdf` `#btnJson`、全部 `select`/`input` 的 `id`、打印时隐藏 `.col-config` 与按钮并显示 `.print-only` 的规则。

---

## 1. 使用建议（怎么用这套提示词最省事）

| 顺序 | 用哪个 | 目的 | 风险 |
| --- | --- | --- | --- |
| 1 | 提示词 D（体检） | 先让 AI 找出「哪里啰嗦、哪里重复、哪些 DOM 可删」 | 无（只读不改） |
| 2 | 提示词 A（速成换肤） | 30 分钟内拿到奥创色，先看效果 | 低（只改 CSS 变量） |
| 3 | 提示词 B（完整皮肤） | 卡片、表单、开关、按钮全面奥创化 | 中（需回归打印/浅色） |
| 4 | 提示词 C（排版简化） | 10 张卡 + 右侧 N 张卡 → 折叠/步骤化 | 高（动 DOM 与 JS 绑定） |
| 5 | 提示词 E（横向三步流） | 终极形态：左侧向导 + 右侧常驻摘要 | 最高（重构渲染逻辑） |
| 6 | 提示词 F（验收） | 改完让 AI 自查，别改坏计算器 | 无 |

**三条铁律**（每个提示词里都已写明，你自己也要守住）：
1. **不碰计算逻辑**：`engine.js` 的公式、数值、置信度标注一个字都不改。
2. **不改 id 与数据属性**：`app.js` 用 `getElementById` 和大量 `querySelector` 绑定，改 id = 页面直接瘫。
3. **保留打印报表**：`@media print` 那一段是导出 PDF 的唯一实现，改版后必须仍然可用。

**给 AI 喂上下文的最佳姿势**：先贴 `index.html` 全文 + `assets/style.css` 全文（约 26KB，任何模型都吃得下），再说「按以下提示词改」；`app.js` 只在提示词 C/E 时才需要贴（它 56KB，可只贴用到的渲染函数）。

---

## 2. 提示词 A —— 速成换肤（只改 CSS 变量，5 分钟见效）

```
你是一位资深前端视觉设计师。我有一个纯静态的「台式机功耗与电源选型计算器」页面，现在要把它改成华硕奥创（Armoury Crate / ROG）的视觉风格。

【项目背景】
- 纯 HTML + CSS + 原生 JS，无框架、无构建步骤。
- 样式全部在 assets/style.css，主题由 :root 里的 CSS 变量驱动。
- 浅色主题通过 html[data-theme="light"] 覆盖同名变量实现（JS 负责切换）。
- 现有变量：
  --bg:#0a0e13; --bg-soft:#0f151c; --panel:#131a23; --panel-2:#18212c;
  --border:#23303f; --border-soft:#1b2532; --text:#e6edf3; --text-dim:#9fb0c3;
  --text-mute:#6b7d92; --accent:#35d0a5; --accent-dim:#1d8f70; --blue:#4c9aff;
  --warn:#f0a92b; --error:#ff5f56; --purple:#a97bff; --radius:10px;
  --radius-sm:7px; --mono:ui-monospace,"SF Mono","Cascadia Mono",Consolas,monospace;
  --shadow:0 8px 28px rgba(0,0,0,.45);

【任务】
只修改 :root 与 html[data-theme="light"] 两组变量值，输出可直接替换的完整变量块（含注释说明每个变量的用途）。不要改其它任何选择器。

【奥创风格色彩规范】
- 底色接近纯黑，带一点点蓝调：#0b0c10 / #101218 / #16181f
- 面板用略带蓝灰的深色，卡片之间靠 1px 微亮描边区分，而不是靠阴影
- 主强调色用奥创的青蓝：#00d4ff（accent），深一档 #0095c8（accent-dim）
- 辅助色：ROG 红 #ff0033 仅用于错误/危险；紫 #a45cff 用于提示；蓝 #4c9aff 保留给次级数据
- 文本：主文本 #e8eaf0，次级 #9aa3b2，弱化 #616a7a
- 浅色主题：底 #f4f6fa、面板 #ffffff、描边 #dfe4ec、主文本 #14161c、强调色加深为 #0077a8（保证白底对比度 ≥4.5:1）
- 圆角统一加大一点：--radius:14px、--radius-sm:10px

【硬性约束】
1. 变量名一个都不能改名、不能删，app.js 依赖 data-theme 切换。
2. 对比度要求：正文文字与背景对比度 ≥ 4.5:1，大号数字 ≥ 3:1。
3. 不引入任何外部依赖（不能用 Google Fonts CDN，不引入 CSS 框架）。
4. 输出格式：直接给两段完整的 CSS 块，我可以整段粘贴替换。
```

---

## 3. 提示词 B —— 完整奥创皮肤（动样式、不动结构）

```
你是一位精通暗色游戏 UI 的前端工程师。请把下面这个纯静态工具页面改成华硕奥创（Armoury Crate / ROG）风格。我会提供 index.html 与 assets/style.css 全文。

【技术前提】
- 纯 HTML + CSS + 原生 JS，无框架无构建，最终产物必须是可直接上传 GitHub Pages 的静态文件。
- 页面主体由 JS 渲染（js/app.js），因此 HTML 里的 class 名和 id 是 JS 的挂钩，不能改。
- 现有 CSS 是「深色为主 + 浅色变量覆盖 + 打印报表」三段式，请保持这个架构。

【奥创风格的设计语言（请严格落地这几点）】
1. 背景：深黑蓝底 + 极淡的对角线/网格纹理（用 CSS repeating-linear-gradient 实现，透明度 ≤3%），营造「设备面板」质感。不要用图片。
2. 卡片：深色半透明面板，1px 描边（rgba(255,255,255,.06)～.08），圆角 14px，无重阴影；hover 时描边微亮 + 极轻上浮（translateY(-1px)）。
3. 卡片标题栏 .card > h2：左侧 .idx 徽标改为「青色渐变方块 + 深色数字」，标题字重 600、letter-spacing .02em，右侧 .hint 保持弱化灰。
4. 强调色：主青 #00d4ff，渐变方向 135deg 从 #00d4ff 到 #0088cc。所有「选中态」（.scenario.on / .seg button.on / .tag.on）改为：青色描边 + 8~12% 青色底 + 数字/文字变青色。
5. 主按钮 button.primary：青色渐变实底 + 深色文字 + 轻微青色外发光（box-shadow: 0 0 0 1px rgba(0,212,255,.35), 0 6px 18px rgba(0,212,255,.18)）；hover 时发光增强。次级按钮 button.ghost 保持透明底 + 描边。
6. 输入控件（select / input[type=text] / input[type=number]）：底比卡片再暗一档，1px 描边，focus 时青色描边 + 3px 青色柔光环。原生下拉箭头在暗色下很难看，请用 background-image 内联 SVG 换成青灰色箭头，并设 appearance:none。
7. 开关 .switch .track：关闭态深灰，开启态青色渐变 + 白色滑块 + 微弱青色光晕。
8. 数字排版：所有大数字（.hero .v、.reco .big、.psu-pick .w）使用等宽字体、font-variant-numeric: tabular-nums、字距收紧，做出「仪表读数」感。#recoBig 是全页最重要的数字，字号要最大（36~40px），并加一层极淡的青色文字辉光。
9. 结果列 .sticky-col 里的 .hero 三宫格：用 1px 分隔线连成一体（而不是三个独立方块），数字颜色区分——规划功耗青、场景预期蓝、瞬时峰值橙。
10. 风险提示 .issue：左侧 3px 色条 + 8% 同色底，.issue.error 用 ROG 红 #ff0033、.warn 用 #ffb020、.ok 用青、.info 用蓝。
11. 顶部 header.top：半透明黑 + backdrop-filter: blur(16px)，底部 1px 青色微光分隔线；logo .bolt 改成青→蓝渐变方块，可加一个 2.5s 无限循环的呼吸动画（respect prefers-reduced-motion）。
12. 滚动条：细、深色、hover 变青。
13. 全局动效克制：所有 transition ≤ 180ms，缓动 cubic-bezier(.4,0,.2,1)；卡片入场可以做极小幅度 fade-up（可选，不要弹跳）。

【必须保留（回归测试清单）】
- html[data-theme="light"] 浅色主题仍然可用且好看，所有新增颜色都必须有浅色版本。
- @media print 打印样式仍然生效：配置列隐藏、按钮隐藏、.print-only 显示、大数字强制黑色、卡片加边框。新增的渐变/辉光在打印时必须被压平成纯色，否则 PDF 会是一团黑。
- 响应式断点：现有 1180px（双列→单列）、640px（grid3/grid2→单列）必须保留；再增加 420px 一档微调内边距与字号。
- .glossary 折叠、.toast 提示、.chk 复选、.storage-row 三列布局功能样式不许缺失。

【输出要求】
1. 先给 200 字以内的设计说明（配色 token、层次逻辑、动效原则）。
2. 再给完整可替换的 assets/style.css（不要省略号、不要「其余保持不变」，我要能整段保存覆盖）。
3. 如果为了实现某个效果必须动 index.html，请单独列出「需修改的 HTML 片段 + 原因」，不要擅自改 id。
```

---

## 4. 提示词 C —— 排版简化（信息架构精简，保留全部功能）

```
你是一位信息架构师 + 前端工程师。下面这个「台式机功耗计算器」功能很全但页面太长、信息过载，我需要你重构排版与信息层次，让它变成一个清爽的向导式工具。功能一个都不能少，但展现在屏幕上的东西要少得多。

【现状问题（我已定位）】
A. 左侧是 10 张连续卡片（场景/CPU/GPU/主板/内存/存储/散热/机箱/扩展/电源），必须从头滚到尾，用户不知道自己在哪一步。
B. 右侧结果列同时塞了：三个核心数字 + 大号推荐瓦数 + 三档推荐 + 兼容性提示 + 升级空间 + 平衡建议 + 功耗明细表 + 导出 + 数据来源，共 9 块，视觉重心分散，用户找不到「我该买多少 W」。
C. 顶部 .banner 是一大段关于数据可靠性的免责说明，用 13px 字号铺满一整条，首次访问者会直接跳过或被劝退。
D. #guideCard 的新手引导和 .glossary 术语表占掉了首屏，且引导文案是「长段落 + 三步骤」，阅读成本高。
E. 大量 .note 说明块（显卡适配主板、PCIe 5.0 SSD、电源校验规则、导出编码说明）散落在各卡片里，每块都是 2~4 行小字，视觉噪音大。
F. #feedbackCard 反馈表单有 6 个字段，占据了页面底部很大空间，但使用频率极低。

【重构方案（请按这个方向实现，细节你可以优化）】
1. 结果优先：把「推荐电源功率」#recoBig 做成整个页面最大的视觉焦点，位置移到结果列最顶部（在 .hero 三宫格之前或与之合并），并配一句人话结论，例如「买 850W 的 ATX 3.1 电源」。三个核心数字 .hero 缩小为辅助一行。
2. 左列步骤化：给 10 张卡加一个步骤指示（沿用 .idx 数字），并让当前卡片在视口内自动高亮（可用 IntersectionObserver 给 .card 加 .is-active，只加 JS 十几行，不动计算逻辑）。同时把「场景 / CPU / 显卡」这三张最关键的卡片默认展开，其余 7 张允许折叠（点击标题栏折叠，默认折叠状态改为「摘要一行」，摘要里显示已选型号，例如「内存 · 32GB DDR5 ×1」）。
3. 顶栏免责声明降级：.banner 改成一行提示条「⚠️ 数据为估算，仅供排除明显不够用的电源 · 查看详情」，点击后展开全文（details/summary 即可）。默认收起。
4. 引导瘦身：#guideCard 保留，但压缩为「一句话 + 两个按钮」，三步骤改成横向三个小图标 + 短标签（选硬件 → 看瓦数 → 查红字）。.glossary 移入一个「术语」按钮弹出的抽屉或直接并入卡片底部折叠区，不再占首屏。
5. 说明块统一收纳：把所有 .note 的详细文字改为「ⓘ」图标 + title/悬浮气泡或折叠，默认只留一句话摘要。禁止删除内容，只做收纳。
6. 反馈表单移出主流程：#feedbackCard 移到页面最底部并在一个 <details> 里，默认收起，标题写「提交新硬件 / 纠错（可选）」。
7. 视觉降噪：统一左侧表单的行高与间距节奏（每张卡内 padding、label 与控件的间距用同一套变量），减少 3 种以上不同的字号；全页字号层级收敛到 4 档（标题 15px / 正文 13px / 辅助 12px / 数字特殊）。
8. 页面总长度目标：1440px 宽的桌面端，首屏必须同时看到「配置入口 + 推荐瓦数」；整页滚动高度比现在减少 30% 以上。

【硬性约束（非常重要）】
1. 绝对不要改任何元素的 id、name、data-* 属性——js/app.js 用 getElementById 绑定事件，改了页面就废了。
2. 不要修改 js/engine.js、js/db*.js 的任何计算逻辑与数据。
3. 折叠/展开只能通过新增 class + CSS + 少量新增 JS 实现，切换开关必须放在「允许新增代码」的范围内：可以新增 js/ui.js（在 index.html 里于 app.js 之前引入），不要往 app.js 里塞大改。
4. 打印导出必须仍然正常：@media print 下配置列隐藏、报表完整、折叠内容必须全部展开（请在 CSS 里加 @media print { details { display:block } details > * { display:block !important } } 之类的保障）。
5. 移动端（<640px）优先：单列、控件触控高度 ≥44px、粘顶结果栏在移动端改为可折叠的底部浮条或顶部迷你条，显示「推荐 850W」+ 展开按钮。
6. 保持无框架、无构建、无外部依赖。

【交付内容】
1. 改造后的完整 index.html。
2. 需要新增/替换的 CSS 片段（标明插入位置或给完整 style.css）。
3. 新增 js/ui.js 的完整代码（步骤高亮、折叠、移动端摘要条）。
4. 一份「我改了什么 + 为什么 + 如何回滚」的 changelog，控制在 20 行以内。
```

---

## 5. 提示词 D —— 精简体检（先诊断，不改代码）

```
你是一位同时懂信息架构和前端的评审员。这是我的「台式机功耗与电源选型计算器」页面源码（index.html 541 行 + assets/style.css 401 行 + js/app.js 负责渲染）。它功能完整，但页面太长、信息密度过高，我打算改版。

请先只做诊断，不要写代码，按以下结构输出：

1. 【首屏分析】1440×900 视口下，用户实际能看到什么？「推荐电源瓦数」这个核心结论出现在第几屏？（请根据 DOM 顺序和 sticky 定位推算，给出具体行号依据）
2. 【信息分层问题】把页面上所有信息块列成表格：块名 / DOM 位置 / 重要度（核心/重要/次要/噪音）/ 建议（保留原样 / 收纳折叠 / 移出主流程 / 删除或合并）/ 理由一句话。
3. 【重复与冗余】找出重复表达的文案或视觉元素（例如同一件事在多处出现），逐条列出。
4. 【视觉噪音清单】数一数全页一共用了多少种字号、多少种间距值、多少个颜色值；指出超过必要的部分和统一建议。
5. 【移动端问题】<640px 时哪些地方会明显崩坏或难用（横向溢出、粘顶区域过高、控件过小、表格列挤压等），按严重程度排序。
6. 【不可动的部分】明确列出哪些 DOM 节点、id、class 因为被 js/app.js 引用而不能改动（请从源码中找出 getElementById / querySelector 的目标）。
7. 【改版优先级】给出一个 P0/P1/P2 的改造清单，每项标注「预计工作量（小时）」与「风险等级」，P0 应该是投入最小、收益最大的三件事。

要求：结论必须基于我提供的源码，引用具体行号或选择器，不要给泛泛的设计建议。
```

---

## 6. 提示词 E —— 终极形态：向导 + 常驻摘要（可选用，工作量最大）

```
你是一位做过大量 SaaS 向导流（wizard）界面的前端架构师。请把这个功耗计算器从「10 张长卡片并列」重构成「分步向导 + 常驻结果摘要」的形态，同时保持华硕奥创的暗色游戏 UI 风格。我会提供完整源码。

【目标交互形态】
- 桌面端（≥1180px）：
  · 左侧 62%：一次只显示一个「步骤面板」，顶部是横向步骤条（1 场景 → 2 CPU → 3 显卡 → 4 主板 → 5 内存 → 6 存储 → 7 散热 → 8 机箱 → 9 扩展 → 10 电源），可点击跳步，已完成步骤打勾。
  · 底部固定「上一步 / 下一步」按钮，最后一步是「生成报告」。
  · 右侧 38%：常驻摘要面板，永远显示当前已选硬件清单（紧凑列表，每行「CPU · i7-14700K · 125W」）+ 三个核心数字 + 最大的推荐瓦数。任何一步的改动都实时刷新（沿用现有计算函数，不要重算逻辑）。
- 移动端（<640px）：步骤面板全屏，底部一条可上滑的「推荐 850W」把手，点开显示摘要。
- 所有硬件选择的控件必须保持原样工作：现有的 select 下拉、.seg 分段按钮、.scenario 场景方块、.switch 开关、动态增删的 .storage-row 与自定义设备行。

【技术约束（这是本次改造的生命线）】
1. 现有 js/app.js 用 getElementById 绑定所有控件、并在数据变化时调用渲染函数刷新结果。请**不要重写 app.js**，而是在此之上新增一层 js/wizard.js（在 app.js 之后引入），它只做三件事：
   a. 把现有 10 个 <section class="card"> 按顺序包装成步骤（用 class 控制显示，例如给容器加 data-step 和 CSS .step-panel:not(.is-current){display:none}）；
   b. 接管「上一步/下一步」的显示切换与步骤条状态；
   c. 监听结果区的 DOM 变化（MutationObserver 监听 .sticky-col 或 .reco 内文本变化），把最新的数字同步到移动端底部把手。
2. 严禁修改任何 id、name、data-*；严禁修改 engine.js / db*.js。
3. 隐藏步骤必须用 CSS 而非 display:none 加在元素上再删除——必须在 @media print 里全部强制展开，保证导出的 PDF 与现在一致。
4. 无框架、无构建、无外部依赖。
5. 键盘可用性：Tab 可遍历，Enter 提交，Esc 关闭移动端把手；焦点样式必须清晰可见（青色 2px outline，不要 outline:none）。
6. prefers-reduced-motion 下关闭所有过渡动画。

【奥创风格落地要点】
- 步骤条：当前步青色渐变胶囊 + 发光，已完成步青色描边打勾，未到达步暗灰。
- 面板切换：淡入 + 8px 上移，180ms，不要横向滑动。
- 右侧摘要：深色半透明面板，顶部一条青色细渐变线；推荐瓦数用 40px 等宽数字 + 青色辉光。
- 三个核心数字用小型数据条（label 上、数字下、单位小字），之间 1px 分隔。

【交付内容】
1. 改造后的 index.html（完整）。
2. 完整 assets/style.css。
3. 新增 js/wizard.js（完整，含注释）。
4. 一段「如何验证没改坏」的手动测试清单（不少于 12 条，覆盖：修改每个硬件后右侧数字是否更新、载入示例、重置、主题切换、导出 CSV/JSON、打印预览、移动端把手、键盘操作）。
```

---

## 7. 提示词 F —— 收尾验收（改完必做）

```
你是代码评审员。我刚对这个静态功耗计算器做了视觉与排版改版。请对照下面这份回归清单，逐条检查我提供的改版后代码，指出任何可能的破坏性改动。

【必须仍然正常的功能】
1. 所有硬件选择控件（场景方块、CPU 下拉、显卡三级联动品牌/型号/AIC、主板、内存、存储增删、散热、风扇、机箱、扩展设备与自定义项、电源下拉）都能改变计算输入。
2. #btnPreset 载入示例配置、#btnReset 重置、#btnTheme 深/浅色切换。
3. #btnQuickStart 一键载入示例并收起引导、#btnHideGuide 收起引导。
4. 导出：#btnCsv 生成 UTF-8 BOM 的 CSV、#btnPdf 调用打印、#btnJson 导出配置 JSON。
5. 反馈表单 #fbAdd 加入待提交列表、#fbCopy 复制 JSON（localStorage 持久化）。
6. 打印/PDF：@media print 下 .col-config 与按钮隐藏、.print-only 显示、报表完整、颜色被压平成打印友好样式。
7. 响应式：1180px 双列→单列、640px 网格→单列，移动端不横向溢出。
8. 无框架依赖、无外链字体、无构建步骤，可直接静态部署。
9. 计算数值与改版前完全一致（视觉改动不得影响任何数字）。
10. 无障碍：对比度达标、焦点可见、prefers-reduced-motion 生效。

【检查方式】
- 从代码中找出所有 getElementById / querySelector / addEventListener 的目标，逐一确认这些 id/class 在新代码中仍然存在且唯一。
- 检查是否引入了重复 id、是否把某个 id 从 DOM 中删掉。
- 检查新增 CSS 是否覆盖了打印样式或浅色主题变量。
- 检查是否有 CSS 选择器因为 DOM 结构变化而失效（例如 .card > h2 的直接子选择器）。

【输出】
一张表格：「问题 / 严重级别（致命·高·中·低）/ 触发条件 / 修复建议（给具体代码）」。没有问题的项也要列出来并标注「通过」。最后给一句总评：这个改版能不能直接部署。
```

---

## 8. 可替换的风格变体（把提示词 B 中的色彩规范换掉即可）

| 风格 | 主色 | 底色 | 适用场景 |
| --- | --- | --- | --- |
| **奥创经典深色**（默认推荐） | `#00d4ff` 青 | `#0b0c10` | 最贴近 Armoury Crate，数字醒目 |
| ROG 红黑 | `#ff0033` 红 | `#0d0d0f` | 更激进的电竞感，但红色作强调色容易和「错误提示」冲突，需把警告色改为琥珀 |
| 奥创浅色 / 银白 | `#0077a8` 深青 | `#f4f6fa` | 白天看得清，但游戏感弱，适合做「浅色主题」 |
| 幻彩渐变 | 青→紫 `#00d4ff → #a45cff` | `#0b0c10` | 只把渐变用在按钮和当前步骤，别用在正文，否则廉价 |

**奥创风格的三个「像不像」关键点**（改完自查）：
1. 底色是不是**纯黑偏蓝**而不是灰？
2. 强调色是不是**单一青色**贯穿全局，而不是五颜六色？
3. 数字是不是**等宽 + 大 + 有微光**，一眼就能看到「850W」？

---

## 9. 最后一句话建议

如果只做一件事：**把 `#recoBig` 那个推荐瓦数变成页面绝对主角**——放大、发光、加一句中文结论（「买 850W 的 ATX 3.1 电源」），并把左列 10 张卡做折叠。这一个改动就能解决「用户找不到答案」的核心问题，成本也最低。
